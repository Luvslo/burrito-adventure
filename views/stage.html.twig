<!DOCTYPE html>
<html>
    <head>
        {% include '/includes/head.html.twig' %}
    </head>
    <body>
        <div class="col-md-8 game_column">
            {% include '/includes/message.html.twig' %}

            <h1>Hi, {{ player.getName }}!</h1>
            <div class="game_div">
                <h3>You are {{ stage.getName }}</h3>
                <p>
                    {{description}}
                </p>

                {% include '/stages/' ~ stage.getId ~ '.html.twig' %}
            </div>

        </div>
        <div class="col-md-4 inventory_column">
            {% include '/includes/inventory.html.twig' %}
        </div>


    </body>
</html>
